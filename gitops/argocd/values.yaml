# Keep only the components needed for Git-driven applications.
dex:
  enabled: false
notifications:
  enabled: false
applicationSet:
  enabled: false
server:
  service:
    type: ClusterIP
  ingress:
    enabled: false

global:
  nodeSelector:
    workload-tier: system
